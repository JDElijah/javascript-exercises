const getTheTitles = function(bookDict) {
    return bookDict.map((book) => book.title); 
};

// Do not edit below this line
module.exports = getTheTitles;

/**
 * The Map object holds key-value pairs and remembers the original insertion order
 * of keys. Any value (both objects and primitive values) may be used as either a key or value. 
 */